---
name: Testing and Linting
on: [push]

jobs:
  test:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - name: Hadolint linting
        run: hadoling Dockerfile
        uses: brpaz/hadolint-action@master
